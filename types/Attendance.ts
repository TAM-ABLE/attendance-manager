// shared/types/Attendance.ts
// 型はすべて shared/lib/schemas.ts からエクスポート（Single Source of Truth）

export type {
  AttendanceRecord,
  Break,
  DailyReportTask,
  Task,
  TaskType,
  User,
  WorkSession,
  WorkSessionUpdate,
} from "../lib/schemas"
