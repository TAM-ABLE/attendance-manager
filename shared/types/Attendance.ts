// shared/types/Attendance.ts
// 型はすべて shared/lib/schemas.ts からエクスポート（Single Source of Truth）

export type {
    Task,
    Break,
    WorkSession,
    WorkSessionUpdate,
    AttendanceRecord,
    User,
    TaskType,
    DailyReportTask,
} from "../lib/schemas";
